{
  "scripts": {
    "build": "node scripts/generate-site.mjs",
    "check": "node scripts/check-site.mjs",
    "test": "playwright test",
    "test:responsive": "playwright test tests/site.spec.ts"
  },
  "dependencies": {
    "cheerio": "^1.2.0",
    "playwright": "^1.61.1"
  },
  "devDependencies": {
    "@playwright/test": "^1.61.1"
  }
}
